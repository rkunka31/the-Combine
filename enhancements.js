(()=>{
const ready=()=>typeof ST!=='undefined'&&typeof NAV!=='undefined'&&typeof setupNav==='function';
function css(){if(document.getElementById('combine-enhancements-css'))return;const s=document.createElement('style');s.id='combine-enhancements-css';s.textContent=`
.payment-summary,.autopsy-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:18px}.payment-card,.grade-card{padding:20px}.payment-grid,.autopsy-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:14px}.payment-row{display:grid;grid-template-columns:44px 1fr auto auto;gap:12px;align-items:center;padding:16px 18px;border-bottom:1px solid var(--line)}.payment-row:last-child{border-bottom:0}.paid-badge{display:inline-flex;padding:5px 9px;border-radius:20px;background:rgba(98,211,148,.12);color:var(--green);font-size:10px;font-weight:700}.draft-trade{display:inline-flex;margin-top:7px;padding:4px 7px;border-radius:6px;background:rgba(215,181,109,.12);color:var(--gold);font-size:9px;line-height:1.35}.draft-pick.traded{border-color:rgba(215,181,109,.45)}.round-roast{padding:20px 0;border-bottom:1px solid var(--line)}.round-roast:last-child{border-bottom:0}.round-roast h2{margin-bottom:9px}.round-roast p{margin:0 0 10px}.grade-card{position:relative}.grade-rank{position:absolute;right:18px;top:16px;font:700 32px var(--display);color:var(--gold)}.grade-card h3{font:600 24px var(--display);text-transform:uppercase;margin:0 50px 3px 0}.grade-card>p{color:var(--muted);margin:0 0 15px}.grade-bars{display:grid;gap:8px}.grade-line{display:grid;grid-template-columns:105px 1fr 36px;gap:8px;align-items:center;font-size:10px;text-transform:uppercase;color:var(--muted)}.grade-line i{height:6px;background:rgba(255,255,255,.07);border-radius:10px;overflow:hidden}.grade-line i:after{content:'';display:block;height:100%;width:var(--score);background:var(--gold);border-radius:10px}.grade-overall{font:700 34px var(--display);color:var(--cream);margin-top:14px}.grade-summary{margin-top:12px!important;color:#cbd5ce!important;line-height:1.65}.feature-actions{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:18px}.owner-line{display:block;color:var(--gold);font-size:11px;margin-bottom:4px}@media(max-width:760px){.payment-summary,.autopsy-summary,.payment-grid,.autopsy-grid{grid-template-columns:1fr}.payment-row{grid-template-columns:38px 1fr}.payment-row .paid-badge{justify-self:start}.grade-line{grid-template-columns:90px 1fr 32px}}
`;document.head.appendChild(s)}
const teamByRoster=id=>ST.rm.get(Number(id));
const who=id=>{const r=teamByRoster(id);return r?`${name(r)} (${owner(r)})`:'Unknown team'};
function transactionOwner(t){const ids=[...(t.roster_ids||[]),...Object.values(t.adds||{}),...Object.values(t.drops||{})].filter(Boolean);const unique=[...new Set(ids.map(Number))];if(t.type==='trade')return unique.map(who).join(' ↔ ');return unique.length?who(unique[0]):'League transaction'}
function enhancedRenderTx(sel,limit){const el=$(sel);if(!el)return;el.innerHTML=ST.tx.length?ST.tx.slice(0,limit).map(t=>{const adds=Object.keys(t.adds||{}).map(pname),drops=Object.keys(t.drops||{}).map(pname),label=t.type==='trade'?'Trade':t.type==='waiver'?'Waiver claim':'Roster move';let detail=adds.length?`Added ${adds.join(', ')}`:drops.length?`Dropped ${drops.join(', ')}`:'Draft assets moved';if(adds.length&&drops.length)detail=`Added ${adds.join(', ')} · Dropped ${drops.join(', ')}`;return `<div class="feed-item"><div class="feed-icon">${t.type==='trade'?'⇄':t.type==='waiver'?'$':'+'}</div><div><span class="owner-line">${esc(transactionOwner(t))}</span><strong>${label}</strong><p>${esc(detail)}</p></div></div>`}).join(''):'<div class="loading-block">No recent transactions.</div>'}
function originalRoster(d,p){const map=d?.slot_to_roster_id||d?.settings?.slot_to_roster_id||{};return Number(map[p.draft_slot]||0)||null}
function pickInfo(p,d){const selected=Number(p.roster_id),original=originalRoster(d,p),traded=!!(original&&selected&&original!==selected);return{pick:p.pick_no,round:p.round,player:`${p.metadata?.first_name||''} ${p.metadata?.last_name||''}`.trim(),position:p.metadata?.position||'',selected,original,traded,team:who(selected),originalTeam:original?who(original):''}}
function localRoundRoast(round,picks){const traded=picks.filter(x=>x.traded),positions=picks.reduce((a,x)=>(a[x.position]=(a[x.position]||0)+1,a),{}),top=Object.entries(positions).sort((a,b)=>b[1]-a[1])[0];let text=`Round ${round} delivered ${picks.length} picks${top?`, led by ${top[1]} ${top[0]} selection${top[1]===1?'':'s'}`:''}. `;text+=traded.length?`${traded.length} pick${traded.length===1?' was':'s were'} acquired in trades, because several managers decided their assigned draft slot was merely a suggestion. `:'Nobody moved a pick in this round, a rare outbreak of patience. ';const first=picks[0];if(first)text+=`${first.team} opened the round with ${first.player}, placing the first target on its own back.`;return text}
function renderDraftRoastLocal(){const d=ST.currentDraft||ST.drafts.find(x=>x.status==='complete')||ST.drafts[0],infos=ST.draftPicks.map(p=>pickInfo(p,d)),by={};infos.forEach(x=>(by[x.round]??=[]).push(x));$('#draft-recap-copy').innerHTML=`<h2>Draft recap and roast</h2><p>${infos.length} picks are on the board. Traded selections are identified using the original draft-slot owner and the roster that made the pick.</p>${Object.entries(by).map(([r,a])=>`<section class="round-roast"><h2>Round ${r}</h2><p>${esc(localRoundRoast(r,a))}</p>${a.map(x=>`<p><strong>${x.pick}. ${esc(x.player)}</strong> · ${esc(x.team)}${x.traded?`<br><span class="draft-trade">Traded from ${esc(x.originalTeam)} to ${esc(x.team)}</span>`:''}</p>`).join('')}</section>`).join('')}`}
async function generateDraftRoast(){const btn=$('#generate-draft-roast'),out=$('#draft-recap-copy'),d=ST.currentDraft||ST.drafts.find(x=>x.status==='complete')||ST.drafts[0],infos=ST.draftPicks.map(p=>pickInfo(p,d));if(!infos.length)return;btn.disabled=true;btn.textContent='Writing the roast...';try{const resp=await fetch(`${CFG.worker}/ai`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model:'claude-opus-4-5',max_tokens:2200,system:'You are a sharp dynasty fantasy football columnist. Return clean HTML using h2, h3, p and strong tags only. Roast every round and specific picks. Be funny and critical without personal attacks. Treat traded draft slots as important context. Never invent a trade or player.',messages:[{role:'user',content:`Write a detailed round-by-round startup draft recap and roast for The Combine. Data: ${JSON.stringify(infos)}. For each round, identify strong values, reaches, positional runs and every traded pick. Explicitly explain original owner versus selecting team when traded.`}]})});if(!resp.ok)throw Error('AI route failed');const data=await resp.json(),html=data.content?.[0]?.text;if(!html)throw Error('No AI text');out.innerHTML=html}catch(e){renderDraftRoastLocal();toast('AI was unavailable. Showing the full data-based roast instead.')}finally{btn.disabled=false;btn.textContent='Regenerate detailed roast'}}
function enhancedRenderDraft(){const d=ST.drafts.find(x=>x.status==='complete')||ST.drafts[0];if(!d){$('#draft-board').innerHTML='<div class="loading-block">No draft found.</div>';return}ST.currentDraft=d;get(`/draft/${d.draft_id}/picks`).then(picks=>{ST.draftPicks=picks;$('#draft-board').innerHTML=`<div class="panel-head"><div><p class="kicker">${esc(d.season||'')} Draft</p><h2>${esc(d.status||'')}</h2></div></div><div class="draft-board-grid">${picks.map(p=>{const x=pickInfo(p,d);return `<div class="draft-pick${x.traded?' traded':''}"><span>Pick ${p.pick_no}</span><strong>${esc(x.player)}</strong><small>${esc(x.position)} · ${esc(x.team)}</small>${x.traded?`<span class="draft-trade">From ${esc(x.originalTeam)}</span>`:''}</div>`}).join('')}</div>`;renderCapital();renderDraftRoastLocal()}).catch(()=>{$('#draft-board').innerHTML='<div class="error-state">Draft data did not load.</div>'})}
function addView(id,html){if(document.getElementById(`view-${id}`))return;document.querySelector('main').insertAdjacentHTML('beforeend',html)}
function paymentView(){const rows=sortRosters();$('#payment-grid').innerHTML=`<div class="panel">${rows.map(r=>`<div class="payment-row"><span class="avatar">${ini(name(r))}</span><div><strong>${esc(name(r))}</strong><small>${esc(owner(r))}</small></div><span class="paid-badge">2026 PAID</span><span class="paid-badge">2027 PAID</span></div>`).join('')}</div>`;$('#payment-summary').innerHTML=`<div class="leader-card"><span>Teams current</span><strong>${rows.length}/${rows.length}</strong><small>2026</small></div><div class="leader-card"><span>Teams current</span><strong>${rows.length}/${rows.length}</strong><small>2027</small></div><div class="leader-card"><span>Outstanding</span><strong>$0</strong><small>All paid</small></div>`}
function rankPercent(values,v,invert=false){const sorted=[...values].sort((a,b)=>a-b),idx=sorted.indexOf(v),pct=sorted.length<2?75:45+50*(idx/(sorted.length-1));return Math.round(invert?145-pct:pct)}
function localGrades(){const rows=sortRosters(),pfs=rows.map(pf),maxes=rows.map(maxpf),pickCounts=new Map(rows.map(r=>[r.roster_id,0]));[2027,2028,2029].forEach(y=>[1,2,3].forEach(round=>rows.forEach(r=>pickCounts.set(r.roster_id,(pickCounts.get(r.roster_id)||0)+1)));ST.picks.forEach(p=>{pickCounts.set(p.roster_id,Math.max(0,(pickCounts.get(p.roster_id)||0)-1));pickCounts.set(p.owner_id,(pickCounts.get(p.owner_id)||0)+1)});return rows.map(r=>{const ps=roster(r),ages=ps.map(x=>Number(x.age)).filter(Boolean),avgAge=ages.length?ages.reduce((a,b)=>a+b,0)/ages.length:27,counts=Object.fromEntries(['QB','RB','WR','TE'].map(x=>[x,ps.filter(p=>p.position===x).length])),dynasty=Math.max(45,Math.min(96,Math.round(94-(avgAge-24)*5))),redraft=Math.round((rankPercent(pfs,pf(r))+rankPercent(maxes,maxpf(r)))/2),balance=Math.max(45,100-(Math.abs(counts.QB-3)*5+Math.abs(counts.RB-6)*4+Math.abs(counts.WR-8)*3+Math.abs(counts.TE-3)*5)),depth=Math.min(96,55+ps.length*1.5),capital=Math.min(98,45+(pickCounts.get(r.roster_id)||0)*5),overall=Math.round(dynasty*.30+redraft*.25+balance*.18+depth*.12+capital*.15);return{roster_id:r.roster_id,team:name(r),owner:owner(r),dynasty,redraft,balance:Math.round(balance),depth:Math.round(depth),capital,overall,players:ps.map(x=>x.full_name).filter(Boolean),counts,avgAge:avgAge.toFixed(1)}}).sort((a,b)=>b.overall-a.overall)}
function localSummary(g){const best=[['dynasty',g.dynasty],['redraft',g.redraft],['balance',g.balance],['depth',g.depth],['capital',g.capital]].sort((a,b)=>b[1]-a[1]),worst=[...best].sort((a,b)=>a[1]-b[1]);return `${best[0][0]} is the selling point. ${worst[0][0]} is where the brochure gets folded shut. The roster has enough structure to compete, but the grade says the group chat should hold the parade plans.`}
function renderGrades(data){$('#autopsy-output').innerHTML=`<div class="autopsy-grid">${data.map((g,i)=>`<article class="panel grade-card"><span class="grade-rank">#${i+1}</span><h3>${esc(g.team)}</h3><p>${esc(g.owner)}</p><div class="grade-bars">${[['Dynasty',g.dynasty],['Redraft',g.redraft],['Balance',g.balance],['Depth',g.depth],['Draft capital',g.capital]].map(([l,v])=>`<div class="grade-line"><span>${l}</span><i style="--score:${v}%"></i><b>${v}</b></div>`).join('')}</div><div class="grade-overall">${g.overall}/100</div><p class="grade-summary">${esc(g.summary||localSummary(g))}</p></article>`).join('')}</div>`;$('#autopsy-summary').innerHTML=`<div class="leader-card"><span>Top roster</span><strong>${esc(data[0]?.team||'')}</strong><small>${data[0]?.overall||0}/100</small></div><div class="leader-card"><span>League average</span><strong>${Math.round(data.reduce((s,x)=>s+x.overall,0)/(data.length||1))}</strong><small>Overall rating</small></div><div class="leader-card"><span>Method</span><strong>5 factors</strong><small>Dynasty · redraft · balance · depth · picks</small></div>`}
async function runAutopsy(){const btn=$('#run-autopsy'),base=localGrades();renderGrades(base);btn.disabled=true;btn.textContent='The coroner is working...';try{const prompt=base.map(g=>({team:g.team,owner:g.owner,players:g.players,position_counts:g.counts,average_age:g.avgAge,future_pick_score:g.capital}));const resp=await fetch(`${CFG.worker}/ai`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model:'claude-opus-4-5',max_tokens:3500,system:'You grade dynasty fantasy football teams. Return ONLY valid JSON, no markdown. Provide an array with exact team names and integer fields dynasty, redraft, balance, depth, capital, overall from 0 to 100, plus a two-sentence roast summary. Use player quality, age, positional scarcity, starting lineup strength, depth and future assets. Be pointed but not cruel.',messages:[{role:'user',content:`Grade and rank these 14 teams: ${JSON.stringify(prompt)}`}]})});if(!resp.ok)throw Error('AI failed');const d=await resp.json(),txt=d.content?.[0]?.text||'',parsed=JSON.parse(txt.replace(/^```json|```$/g,'').trim());const map=new Map(parsed.map(x=>[x.team,x]));const merged=base.map(g=>({...g,...(map.get(g.team)||{})})).sort((a,b)=>b.overall-a.overall);renderGrades(merged)}catch(e){toast('AI grading was unavailable. Showing the data-based roster autopsy.')}finally{btn.disabled=false;btn.textContent='Run AI autopsy again'}}
function patchCharter(){const data=window.COMBINE_CHARTER;if(!data)return;const s=data.sections.find(x=>x.id==='section-3');if(!s)return;const lineup=s.blocks.find(x=>x.title?.startsWith('3.1'));if(lineup)lineup.rows=[['Quarterback','1',''],['Running Back','2',''],['Wide Receiver','3',''],['Tight End','1','Dedicated starter'],['Flex, RB/WR/TE','2','Any combination'],['Total Starters','9','14-team league']];const full=s.blocks.find(x=>x.title?.startsWith('3.2'));if(full)full.rows=[['Active Roster, starters plus bench','23','All players'],['Starters','9','See Section 3.1'],['Bench','14','All players'],['Taxi Squad','5','Rookies and Year 2 players, offseason additions only'],['Injured Reserve','5','Official IR designation only']];if(typeof window.charter==='function'&&document.getElementById('charter-copy'))window.charter()}
function install(){if(!ready())return setTimeout(install,50);css();patchCharter();if(!NAV.some(x=>x[0]==='payments'))NAV.splice(NAV.length-2,0,['payments','Payments']);if(!NAV.some(x=>x[0]==='autopsy'))NAV.splice(NAV.length-2,0,['autopsy','Roster Autopsy']);addView('payments',`<section class="view" id="view-payments"><div class="page-head shell"><p class="eyebrow">LEAGUE TREASURY</p><h1>Payment tracker</h1><p>Entry-fee status by team and season.</p></div><div class="shell"><div id="payment-summary" class="leader-cards"></div><div id="payment-grid"></div></div></section>`);addView('autopsy',`<section class="view" id="view-autopsy"><div class="page-head shell"><p class="eyebrow">THE ROSTER MORGUE</p><h1>Roster Autopsy</h1><p>Dynasty value, redraft strength, positional balance, depth and draft capital. Every team leaves with a grade and a bruise.</p></div><div class="shell"><div class="feature-actions"><button class="primary-button" id="run-autopsy">Run AI roster autopsy</button></div><div id="autopsy-summary" class="leader-cards"></div><div id="autopsy-output" class="loading-block">Loading team evidence...</div></div></section>`);setupNav();renderTx=enhancedRenderTx;renderDraft=enhancedRenderDraft;renderRecap=renderDraftRoastLocal;$('#generate-draft-roast').onclick=generateDraftRoast;$('#run-autopsy').onclick=runAutopsy;paymentView();renderGrades(localGrades());if(ST.tx.length){enhancedRenderTx('#dashboard-activity',5);enhancedRenderTx('#activity-feed',40)}if(ST.drafts.length)enhancedRenderDraft()}
install();
})();